{
  "version": "0.0.0-development",
  "license": "MIT",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "files": [
    "dist",
    "src"
  ],
  "engines": {
    "node": ">=10"
  },
  "scripts": {
    "start": "tsdx watch",
    "build": "tsdx build",
    "test": "tsdx test",
    "format": "prettier --check 'src/**/*.+(json|less|css|md|gql|graphql|html|yaml)' package.json",
    "lint": "eslint --cache --max-warnings 0 'src/**/*.+(js|jsx|ts|tsx|vue)'",
    "lint:report": "eslint --cache --max-warnings 0 --output-file eslint_report.json --format json 'src/**/*.+(js|jsx|ts|tsx|vue)'",
    "prepare": "tsdx build",
    "xlint:report": "yarn lint --output-file eslint_report.json --format json",
    "xlint:report2": "eslint --ext .ts --output-file eslint_report.json --format json src",
    "readme": "devkeeper-docs readme",
    "docs:dev": "devkeeper-docs vuepress-dev",
    "docs:build": "devkeeper-docs vuepress"
  },
  "peerDependencies": {},
  "name": "@ozum/sil",
  "author": "Özüm Eldoğan",
  "module": "dist/sil.esm.js",
  "devDependencies": {
    "@commitlint/cli": "^8.3.5",
    "@commitlint/config-conventional": "^8.3.4",
    "cz-conventional-changelog": "^3.2.0",
    "eslint-config-devkeeper": "^1.0.9",
    "husky": "^4.2.5",
    "lint-staged": ">=10",
    "tsdx": "^0.13.2",
    "tslib": "^1.11.2",
    "typescript": "^3.8.3"
  },
  "dependencies": {},
  "husky": {
    "hooks": {
      "xpre-commit": "lint-staged",
      "prepare-commit-msg": "exec < /dev/tty && git cz --hook || true",
      "commit-msg": "commitlint -E HUSKY_GIT_PARAMS"
    }
  },
  "commitlint": {
    "extends": [
      "@commitlint/config-conventional"
    ]
  },
  "lint-staged": {
    "*.{jsx,tsx,vue}": [
      "eslint --cache --max-warnings 0"
    ],
    "*.{js,ts}": [
      "eslint --cache --max-warnings 0",
      "jest --bail --coverage --findRelatedTests --config=jest.config.json"
    ],
    "*.{json,less,css,md,gql,graphql,html,yaml}": [
      "prettier --write"
    ],
    ".*{rc, json}": "jsonlint"
  },
  "config": {
    "commitizen": {
      "path": "cz-conventional-changelog"
    }
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/ozum/sil.git"
  }
}
